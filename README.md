### installation

##### First step

 `composer install` 
 ##### Second step
Set up the database connection

##### Third step
`php artisan migrate`

### Test
Third step run the phpunit test command
`./vendor/bin/phpunit`
